# Changelog Options

As for now, each of the options that is used by `cz changelog` command can correlate to a command line option.

See [changelog command line options](https://commitizen-tools.github.io/commitizen/commands/changelog/#command-line-options) for more details.
